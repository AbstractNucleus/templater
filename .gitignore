.DS_Store
node_modules/
/build
.svelte-kit/
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust — keep Cargo.lock since this is an application, not a library
/src-tauri/target/

# Bundled Node binary (downloaded by scripts/fetch-node-binary.ts)
/src-tauri/binaries/

# Editor / OS junk
Thumbs.db
*.swp
*.swo
.vscode/
.claude/

# Engagement / private seed data — not for publication
*.csv

# Local codebase visualization
codebase-map.html
